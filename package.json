{
  "name": "staybase",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "concurrently -n api,web -c blue,magenta \"npm run dev --workspace backend\" \"npm run dev --workspace frontend\"",
    "build": "npm run build --workspace backend",
    "db:reset": "echo 'De app draait op Supabase Postgres — beheer de data via het Supabase-dashboard (backend/data/staybase.db is enkel nog een backup van vóór de migratie).'",
    "start": "npm run start --workspace backend"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
