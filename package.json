{
  "name": "staybase",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "concurrently -n api,web -c blue,magenta \"npm run dev --workspace backend\" \"npm run dev --workspace frontend\"",
    "build": "npm run build --workspace backend && npm run build --workspace frontend",
    "db:reset": "rm -f backend/data/staybase.db && echo \"Database gereset — de seed draait opnieuw bij de volgende start.\""
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
